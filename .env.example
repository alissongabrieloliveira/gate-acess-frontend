VITE_API_URL=http://localhost:3333/api/v1

# Opcional — DSN do Sentry (Settings > Projects > seu projeto > Client Keys no
# painel do Sentry). Mesmo projeto/organização do backend, um DSN diferente
# (projeto Sentry separado, "plataforma" React em vez de Node). Sem ele,
# rastreamento de erro do frontend fica desativado (a app builda/sobe
# normalmente do mesmo jeito).
VITE_SENTRY_DSN=
